{
  "name": "trellis",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "tauri": "pnpm --filter @trellis/shell tauri",
    "reset:client": "node scripts/reset-client.mjs"
  },
  "devDependencies": {
    "turbo": "^2.10.12",
    "typescript": "^7.0.2"
  }
}
